#include <stdio.h>

int main() {
    int n, i = 1, triangular = 0;

    printf("Enter n: ");
    scanf("%d", &n);

    printf("First %d triangular numbers:\n", n);

    while (i <= n) {
        triangular += i;
        printf("%d ", triangular);
        i++;
    }

    printf("\n");

    return 0;
}


How it Works
1. Ask the user f
